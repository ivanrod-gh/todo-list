{
  "name": "node-server07",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start:general:dev": "cd general && NODE_ENV=development nest start --watch",
    "start:auth:dev": "cd auth && NODE_ENV=development nest start --watch",
    "start:dev": "concurrently \"npm run start:general:dev\" \"npm run start:auth:dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
